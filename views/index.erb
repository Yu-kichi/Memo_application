<div class = "text-center">
  <h1 class = "mt-4" >メモアプリ</h1>
  <div class ="mt-4">
    <a class = "btn btn-primary m-4 " href="/memos/new"><i class="fas fa-pen"></i>新規MEMO作成</a>
  </div>
  <table class="table table-striped　table-borderless">
  <thead>
    <tr>
      <th>タイトル</th>
      <th>内容</th>
      <th>更新日時</th>
    </tr>
  </thead>
    <tbody>
    <% @memos.each do |memo|%>
    <tr>
      <td><a href="/memos/<%= memo['memo_id'] %>"><%= memo['memo_title'] %></td>
      <td><%= convert_to_br(memo['memo_body']) %></td>
      <td><%= memo['updated_at'] %></td>
      <br>
    </tr>
    <% end %> 
    </tbody>
  </table>
</div>
